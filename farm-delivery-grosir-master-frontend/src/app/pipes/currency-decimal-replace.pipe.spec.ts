import { CurrencyDecimalReplacePipe } from './currency-decimal-replace.pipe';

describe('CurrencyDecimalReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyDecimalReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
